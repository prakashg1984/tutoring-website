{
  "name": "tutoring-website",
  "version": "1.0.0",
  "description": "Tutoring website with React and Express",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "install-all": "npm install && cd frontend && npm install",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "nodemailer": "^6.9.7",
    "dotenv": "^16.3.1"
  }
}
